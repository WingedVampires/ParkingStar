<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_user_label"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_marginStart="20dp"
        app:layout_constraintBottom_toBottomOf="@id/tv_user_title"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_user_title" />

    <TextView
        android:id="@+id/tv_user_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:textColor="#572C2C"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/iv_user_label"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="真实姓名" />

    <EditText
        android:id="@+id/et_user_content"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginEnd="30dp"
        android:background="@null"
        android:hint="nin还没有填写呢"
        android:layout_marginStart="60dp"
        android:gravity="end"
        android:imeOptions="actionNext"
        android:singleLine="true"
        app:layout_constraintLeft_toRightOf="@id/tv_user_title"
        android:textColor="#333333"
        android:textSize="20sp"

        app:layout_constraintBottom_toBottomOf="@id/tv_user_title"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_user_title" />

    <Spinner
        android:id="@+id/sp_user_content"
        android:layout_width="150dp"
        android:layout_height="0dp"
        android:layout_marginEnd="30dp"
        android:gravity="end"
        android:textColor="#333333"
        android:textSize="20sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/tv_user_title"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_user_title" />

    <TextView
        android:id="@+id/tv_user_content"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_marginEnd="30dp"
        android:text="$nin还没有填写呢￥"
        android:textColor="#333333"
        android:textSize="20sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/tv_user_title"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_user_title" />

</android.support.constraint.ConstraintLayout>

